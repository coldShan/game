function drawBackground() { //绘制背景
    // bgImg.onload = function () { //背景图片加载完成后才能绘制出来
    // ctx2.drawImage(bgImg, 0, 0, canWidth, canHeight);
    // }
    ctx2.drawImage(bgImg, 0, 0, canWidth, canHeight);
}